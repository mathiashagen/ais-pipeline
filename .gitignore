# Build output
/build/
/out/

# CMake user files
CMakeUserPresets.json

# Runtime database. WAL mode adds -wal and -shm alongside it, and a -wal file
# can hold committed rows.
ais_data.db*

# Editor / IDE
.vs/
*.user
# .vscode/ is intentionally tracked — shared project config lives there.

# OS
Thumbs.db
.DS_Store
