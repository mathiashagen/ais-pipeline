#pragma once

namespace ais {

// Placeholder so we can verify compile + link + test before any real logic
// exists. Replace this with the decoder API in Fase 1.
const char* library_version() noexcept;

}  // namespace ais
